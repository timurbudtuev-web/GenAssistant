# GenAssistant 2.0 Desktop

Десктопное приложение GenAssistant 2.0 для МегаФона на базе Electron.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd genassistant-desktop
npm install
```

### 2. Запуск в режиме разработки

```bash
npm start
```

### 3. Сборка для продакшена

**Windows:**
```bash
npm run build:win
```

**macOS:**
```bash
npm run build:mac
```

**Linux:**
```bash
npm run build:linux
```

Готовые установщики появятся в папке `dist/`

## 📁 Структура проекта

```
genassistant-desktop/
├── main.js              # Главный процесс Electron
├── index.html           # Основной HTML файл
├── package.json         # Конфигурация проекта
├── src/
│   ├── styles/          # CSS файлы (можно разделить)
│   ├── scripts/         # JavaScript модули (можно разделить)
│   └── components/      # Компоненты UI
└── assets/              # Иконки и ресурсы
```

## 🔧 Конфигурация

### API ключ Anthropic

⚠️ **Важно**: Перед использованием замените API ключ в файле `index.html`:

Найдите строку (около 4620):
```javascript
"x-api-key": "ВАШ_API_КЛЮЧ_ЗДЕСЬ"
```

### Настройки окна

В `main.js` можно изменить размеры и поведение окна:
```javascript
width: 1400,
height: 900,
minWidth: 1200,
minHeight: 700
```

## 📦 Сборка установщика

### Windows (.exe)
```bash
npm run build:win
```
Создаст NSIS установщик в `dist/GenAssistant Setup 2.0.0.exe`

### macOS (.dmg)
```bash
npm run build:mac
```
Создаст DMG образ в `dist/GenAssistant-2.0.0.dmg`

### Linux (.AppImage)
```bash
npm run build:linux
```
Создаст AppImage в `dist/GenAssistant-2.0.0.AppImage`

## 🎨 Особенности Desktop версии

✅ **Нативное окно** - работает как обычное приложение Windows/Mac/Linux
✅ **Офлайн работа** - все данные хранятся локально
✅ **Быстрый запуск** - не нужен браузер
✅ **Системные уведомления** - интеграция с ОС
✅ **Автообновления** - можно добавить через electron-updater

## 🔐 Безопасность

- API ключ должен храниться в переменных окружения
- Для продакшена используйте electron-store для шифрования
- Рекомендуется добавить аутентификацию пользователей

## 📝 Следующие шаги

1. **Вынести API ключ в конфиг**
   - Создать `.env` файл
   - Использовать `process.env.API_KEY`

2. **Разделить код на модули**
   - Вынести стили в отдельные CSS файлы
   - Разбить JavaScript на модули
   - Создать отдельные компоненты

3. **Добавить иконки**
   - Создать иконки для Windows (.ico)
   - Создать иконки для macOS (.icns)
   - Создать иконки для Linux (.png)

4. **Настроить автообновления**
   ```bash
   npm install electron-updater
   ```

## 🐛 Troubleshooting

### CORS ошибки
В desktop версии CORS проблем нет, но если возникают:
- Проверьте nodeIntegration в main.js
- Убедитесь что contextIsolation: false

### Не работают Computer Use инструменты
- В desktop версии нужен локальный сервер для выполнения команд
- Или интеграция с реальным Computer Use API

## 📞 Поддержка

Для вопросов по десктопной версии обращайтесь к команде разработки МегаФона.

---

**Версия:** 2.0.0  
**Лицензия:** MIT  
**© 2025 МегаФон**
